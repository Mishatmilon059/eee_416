# 📖 বাংলা ব্রেইল টিউটর (Bangla Braille Tutor) — টিমমেট ইউজার ম্যানুয়াল

স্বাগতম! এটি **EEE-416** প্রজেক্টের AI ডাটা কালেকশন অ্যাপের অফিসিয়াল নির্দেশিকা। ব্রেইল জানা না থাকলেও যে কেউ এই অ্যাপের মাধ্যমে বাংলা ব্রেইল প্র্যাকটিস করতে পারবেন এবং আমাদের AI মডেলের জন্য ডাটা প্রদান করতে পারবেন।

---

## 🌐 ১. অ্যাপে প্রবেশ করার উপায়
অ্যাপটি যেকোনো পিসি, ল্যাপটপ, ট্যাবলেট বা স্মার্টফোনের ব্রাউজার থেকে সরাসরি ওপেন করা যায়:
👉 **https://eee-416.vercel.app/web/** *(বা আপনার Vercel লাইভ লিংক)*

---

## 🎯 ২. আপনি ব্রেইল না জানলেও যেভাবে টেস্ট দিবেন
অ্যাপের স্ক্রিনে প্রতিটি বাংলা অক্ষরের পাশে **৬-ডটের একটি ব্রেইল ঘর** দেখানো থাকে। 
* যেই ডটটি নীল হয়ে জ্বলে থাকবে, সেটিই হলো ঐ অক্ষরের সঠিক উত্তর!
* আপনাকে কেবল সেই ডট বাটনগুলো সিলেক্ট করে **Submit** করতে হবে।

---

## ⌨️ ৩. ইনপুট দেয়ার নিয়ম (ডিভাইস অনুযায়ী)

### 💻 ল্যাপটপ বা পিসি ব্যবহারকারীদের জন্য (২টি উপায়):

#### 🔹 অপশন এ: Numpad 3x2 Grid (সর্বোত্তম ও সবচেয়ে সহজ)
কিবোর্ডের ডানপাশের Numpad ব্যবহার করুন (ঠিক ব্রেইল ঘরের আকৃতিতে):
```text
  [ Dot 1 ]  ( 7 )   |   ( 8 )  [ Dot 4 ]
  [ Dot 2 ]  ( 4 )   |   ( 5 )  [ Dot 5 ]
  [ Dot 3 ]  ( 1 )   |   ( 2 )  [ Dot 6 ]
```
* **বাম পাশের ডট (১, ২, ৩):** Numpad `7`, `4`, `1`
* **ডান পাশের ডট (৪, ৫, ৬):** Numpad `8`, `5`, `2`
* **জমা দিন (Submit):** `Spacebar` (স্পেসবার)

#### 🔹 অপশন বি: Standard Perkins Keyboard
* **বাম হাত:** `F` (ডট ১), `D` (ডট ২), `S` (ডট ৩)
* **ডান হাত:** `J` (ডট ৪), `K` (ডট ৫), `L` (ডট ৬)

---

### 📱 মোবাইল বা ট্যাবলেট ব্যবহারকারীদের জন্য:
* স্ক্রিনে দেখানো ডট বাটনগুলোতে (Dot 1, Dot 2, Dot 3...) সরাসরি আঙুল দিয়ে **ট্যাপ (Tap)** করুন।
* ইনপুট শেষে নিচে থাকা নীল **Submit** বাটনে ট্যাপ করুন।

---

## 🚀 ৪. সেশন শুরুর ধাপসমূহ (Step-by-Step)

1. **Participant Code দিন:** অ্যাপ ওপেন করে আপনার বা অংশগ্রহণকারীর নাম/কোড লিখুন (যেমন: `P01`, `P02` ইত্যাদি)।
2. **সেশন শুরু করুন:** **"Start session"** বাটনে ক্লিক করুন।
3. **অক্ষর প্র্যাকটিস করুন:** স্ক্রিনে আসা অক্ষরের নির্দেশিকা দেখে ডট সিলেক্ট করুন এবং **Submit** বা **Spacebar** চাপুন।
4. **সেশন শেষ:** নির্ধারিত সংখ্যার উত্তর দেওয়া শেষ হলে সেশন অটোমেটিক সম্পন্ন হবে।

---

## ⚠️ 🖐️ ডাটা কালেকশনের গুরুত্বপূর্ণ নিয়মাবলী (টিমমেটদের জন্য)

1. **ভিন্ন দিনে প্র্যাকটিস করা (Spread across days):**
   * আমাদের AI মডেল শেখার গতি ও সময় ট্র্যাকিং করে। তাই একই মানুষ একদিনে সব সেশন না দিয়ে **কমপক্ষে ২-৩ দিনে আলাদা সেশনে** প্র্যাকটিস করলে ডাটা বেশি কার্যকর হয়।
2. **ডাটা স্বয়ংক্রিয় সেভ:** 
   * আপনার প্রতিটি ইনপুট সাথে সাথে ডাটাবেজে জমা হয়ে যায়। আলাদাভাবে কিছু সেভ করতে হবে না।
3. **ইন্টারনেট সংযোগ:** 
   * সাময়িক ইন্টারনেট না থাকলেও সমস্যা নেই, অ্যাপটি ডাটা ধরে রাখবে এবং ইন্টারনেট পাওয়ার সাথে সাথে ব্যাকএন্ডে সিঙ্ক করে দেবে।

---

**ধন্যবাদ আমাদের প্রজেক্টে ডাটা দিয়ে সাহায্য করার জন্য! 💖**
